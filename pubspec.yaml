version: 3.0.3
name: separated_column
description: Flutter package for rendering Column widget that injects the separator in between the children.
homepage: https://github.com/anisalibegic/separated_column
repository: https://github.com/anisalibegic/separated_column
issue_tracker: https://github.com/anisalibegic/separated_column/issues

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

screenshots:
  - description: Example usage.
    path: screenshots/example.gif